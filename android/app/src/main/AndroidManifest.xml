<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:label="Huzur Vakti"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        
        <!-- Widget 1: Kompakt Namaz Vakti (2x1) -->
        <receiver 
            android:name=".widgets.KompaktVakitWidget"
            android:exported="true"
            android:label="@string/widget_kompakt_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_kompakt_info" />
        </receiver>
        
        <!-- Widget 2: Günlük Vakitler (2x2) -->
        <receiver 
            android:name=".widgets.GunlukVakitlerWidget"
            android:exported="true"
            android:label="@string/widget_gunluk_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_gunluk_info" />
        </receiver>
        
        <!-- Widget 3: Premium Sayaç (3x2) -->
        <receiver 
            android:name=".widgets.PremiumSayacWidget"
            android:exported="true"
            android:label="@string/widget_premium_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_premium_info" />
        </receiver>
        
        <!-- Widget 4: Minimal Daire (2x2) -->
        <receiver 
            android:name=".widgets.MinimalDaireWidget"
            android:exported="true"
            android:label="@string/widget_minimal_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_minimal_info" />
        </receiver>
        
        <!-- Widget 5: Günün Sözü (4x1) -->
        <receiver 
            android:name=".widgets.GununSozuWidget"
            android:exported="true"
            android:label="@string/widget_sozu_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_sozu_info" />
        </receiver>
        
        <!-- Widget 6: Esmaül Hüsna (2x1) -->
        <receiver 
            android:name=".widgets.EsmaulHusnaWidget"
            android:exported="true"
            android:label="@string/widget_esma_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_esma_info" />
        </receiver>
        
        <!-- Widget 7: Kıble Pusulası (2x2) -->
        <receiver 
            android:name=".widgets.KibleWidget"
            android:exported="true"
            android:label="@string/widget_kible_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_kible_info" />
        </receiver>
        
        <!-- Widget 8: İki Vakit (2x2) -->
        <receiver 
            android:name=".widgets.IkiVakitWidget"
            android:exported="true"
            android:label="@string/widget_iki_vakit_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_iki_vakit_info" />
        </receiver>
        
        <!-- Widget 9: Ramazan Sayaç (3x2) -->
        <receiver 
            android:name=".widgets.RamazanSayacWidget"
            android:exported="true"
            android:label="@string/widget_ramazan_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_ramazan_info" />
        </receiver>
        
        <!-- Widget 10: Zikir Sayaç (2x2) -->
        <receiver 
            android:name=".widgets.ZikirSayacWidget"
            android:exported="true"
            android:label="@string/widget_zikir_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.example.huzur_vakti.ACTION_INCREMENT" />
                <action android:name="com.example.huzur_vakti.ACTION_DECREMENT" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_zikir_info" />
        </receiver>
        
        <!-- Widget 11: Cuma Sayaç (3x2) -->
        <receiver 
            android:name=".widgets.CumaSayacWidget"
            android:exported="true"
            android:label="@string/widget_cuma_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_cuma_info" />
        </receiver>
        
        <!-- Widget 12: Haftalık Vakitler (3x3) -->
        <receiver 
            android:name=".widgets.HaftalikVakitWidget"
            android:exported="true"
            android:label="@string/widget_haftalik_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_haftalik_info" />
        </receiver>
        
        <!-- Widget 13: Günün Ayeti (4x2) -->
        <receiver 
            android:name=".widgets.GununAyetiWidget"
            android:exported="true"
            android:label="@string/widget_ayet_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_ayet_info" />
        </receiver>
        
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
